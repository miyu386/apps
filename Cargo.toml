[workspace]
resolver = "2"

members = [
    "fungible-token",
    "gear-feeds-router",
    "ping",
    "fungible-token-messages",
    "non-fungible-token",
    "curve-amm"
]

[profile.release]
lto = true
opt-level = "s"
